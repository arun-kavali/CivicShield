import mongoose from 'mongoose';
const incidentSchema = new mongoose.Schema({ organizationId: { type: mongoose.Schema.Types.ObjectId, ref: 'Organization', required: true, index: true }, incidentId: { type: String, required: true, unique: true }, title: { type: String, required: true }, priority: { type: String, enum: ['Low', 'Medium', 'High', 'Critical'], required: true }, status: { type: String, enum: ['Open', 'Investigating', 'Contained', 'Resolved', 'Closed'], default: 'Open' }, summary: String, affectedAssets: [String], alerts: [{ type: mongoose.Schema.Types.ObjectId, ref: 'Alert' }], assignedAnalyst: { type: mongoose.Schema.Types.ObjectId, ref: 'User' }, riskScore: Number, threatConfidenceScore: Number, classification: String, containmentActions: [mongoose.Schema.Types.Mixed], resolutionSummary: String }, { timestamps: true });
export const Incident = mongoose.model('Incident', incidentSchema);
